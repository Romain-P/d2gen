#ifndef {{ x.name.toUpperCase() }}_H_
#define {{ x.name.toUpperCase() }}_H_

enum {{ x.name }} {
{% for name, field in x.fields %}
    {{ name }} = {{ field.value }}{{ (loop.last) ? '' : ',' }}
{% endfor %}
}

#endif /** {{ x.name.toUpperCase() }}_H_ **/